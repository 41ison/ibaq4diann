% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/count_theoretical_peptides.R
\name{count_theoretical_peptides}
\alias{count_theoretical_peptides}
\title{Count theoretically observable tryptic peptides for a protein sequence}
\usage{
count_theoretical_peptides(
  sequence,
  protease_regex = "(?<=[KR])(?!P)",
  min_len = 6,
  max_len = 30,
  max_missed = 0
)
}
\arguments{
\item{sequence}{\code{character(1)}. A single amino-acid sequence as a plain
character string (e.g. \code{"MKTIIALSYIFCLVFA"}). \code{NA} or an empty string
returns \code{NA_integer_}.}

\item{protease_regex}{\code{character(1)}. A Perl-compatible regular expression
that matches \strong{cut sites} (positions between residues where cleavage
occurs). Default: \code{"(?<=[KR])(?!P)"} (trypsin, no-cleavage before Pro).}

\item{min_len}{\code{integer(1)}. Minimum peptide length (number of residues) to
include in the count. Default: \code{6}.}

\item{max_len}{\code{integer(1)}. Maximum peptide length (number of residues) to
include in the count. Default: \code{30}.}

\item{max_missed}{\code{integer(1)}. Maximum number of missed cleavages allowed.
\code{0} = fully cleaved peptides only. Default: \code{0}.}
}
\value{
\code{integer(1)}. The number of unique theoretically observable peptides
within \verb{[min_len, max_len]} after \emph{in-silico} digestion. Returns
\code{NA_integer_} when \code{sequence} is \code{NA} or empty.
}
\description{
Performs an \emph{in-silico} digest of a single amino-acid sequence using a
configurable cleavage rule (default: trypsin), then counts the number of
\strong{unique} fully-cleaved peptides whose length falls within
\verb{[min_len, max_len]}. This count is the denominator in the iBAQ formula.

The default trypsin rule cuts after lysine (K) or arginine (R) \strong{unless}
the next residue is proline (P), i.e., the regex look-behind/ahead
\verb{(?<=[KR])(?!P)}.
}
\seealso{
\code{\link[=build_theoretical_peptide_counts]{build_theoretical_peptide_counts()}}, \code{\link[=compute_ibaq]{compute_ibaq()}}
}
\keyword{internal}
