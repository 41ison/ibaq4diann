% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_ibaq_distribution.R
\name{plot_ibaq_distribution}
\alias{plot_ibaq_distribution}
\title{Plot iBAQ value distributions across samples}
\usage{
plot_ibaq_distribution(
  ibaq,
  samples = NULL,
  plot_type = c("violin", "boxplot", "both"),
  log2_values = TRUE,
  fill_alpha = 0.6,
  color_palette = NULL
)
}
\arguments{
\item{ibaq}{A \code{data.frame} or \code{tibble} returned by \code{\link[=compute_ibaq]{compute_ibaq()}}, in
either wide or long format.}

\item{samples}{\code{character} vector or \code{NULL}. Samples to include. When
\code{NULL} (default), all samples are shown.}

\item{plot_type}{\code{character(1)}. One of \code{"violin"} (default), \code{"boxplot"},
or \code{"both"} (violin with overlaid box plot).}

\item{log2_values}{\code{logical(1)}. Are values already log2-transformed?
Affects only the y-axis label. Default: \code{TRUE}.}

\item{fill_alpha}{\code{numeric(1)}. Fill opacity for violin/box shapes (0–1).
Default: \code{0.6}.}

\item{color_palette}{\code{character} or \code{NULL}. Named vector mapping sample
names to fill colours, or \code{NULL} to use the default ggplot2 palette.
Default: \code{NULL}.}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2}} object.
}
\description{
Draws a violin plot, box plot, or both overlaid for the distribution of
iBAQ values across samples. Useful for quality-control checks such as
assessing between-sample normalisation and detecting outlier runs.
}
\examples{
\dontrun{
ibaq <- compute_ibaq("report.parquet", "proteome.fasta")

# Violin plot for all samples
plot_ibaq_distribution(ibaq)

# Box plot for selected samples
plot_ibaq_distribution(
  ibaq,
  samples = c("sample_A", "sample_B", "sample_C"),
  plot_type = "boxplot"
)

# Both violin + box
p <- plot_ibaq_distribution(ibaq, plot_type = "both")
ggplot2::ggsave("ibaq_distributions.pdf", p, width = 10, height = 6)
}
}
