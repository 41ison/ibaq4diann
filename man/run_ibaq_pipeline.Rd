% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_ibaq_pipeline.R
\name{run_ibaq_pipeline}
\alias{run_ibaq_pipeline}
\title{Run the full iBAQ pipeline and write results to disk}
\usage{
run_ibaq_pipeline(
  parquet_path = "report.parquet",
  fasta_path = "proteome.fasta",
  output_path = "iBAQ_results.tsv",
  ...
)
}
\arguments{
\item{parquet_path}{\code{character(1)}. Path to the DIA-NN \code{report.parquet}
file. Default: \code{"report.parquet"} (current working directory).}

\item{fasta_path}{\code{character(1)}. Path to the reference proteome FASTA.
Default: \code{"proteome.fasta"} (current working directory).}

\item{output_path}{\code{character(1)}. Destination path for the output TSV file.
Parent directories must already exist. Default: \code{"iBAQ_results.tsv"}.}

\item{...}{Additional arguments forwarded verbatim to \code{\link[=compute_ibaq]{compute_ibaq()}}.
Use these to override any default filtering, digestion, or transformation
parameters (e.g. \code{log2_transform = FALSE}, \code{output_long = TRUE}).}
}
\value{
Invisibly returns the iBAQ \link[tibble:tibble]{tibble} (same as
\code{\link[=compute_ibaq]{compute_ibaq()}}). The primary side-effect is writing a TSV file to
\code{output_path}.
}
\description{
Convenience wrapper that calls \code{\link[=compute_ibaq]{compute_ibaq()}} with the specified
arguments, writes the resulting iBAQ table to a tab-separated file, and
invisibly returns the tibble for further use in R.

This function is intended for straightforward, end-to-end runs where the
user wants to compute iBAQ values and immediately save them to disk without
further customisation. For finer control over individual steps, call
\code{\link[=compute_ibaq]{compute_ibaq()}} directly.
}
\examples{
\dontrun{
# ── Minimal call (all defaults) ─────────────────────────────────────────────
run_ibaq_pipeline(
  parquet_path = "report.parquet",
  fasta_path = "UP000031575.fasta"
)

# ── Save to a custom path with extra parameters ─────────────────────────────
result <- run_ibaq_pipeline(
  parquet_path = "data/report.parquet",
  fasta_path = "data/proteome.fasta",
  output_path = "results/iBAQ_results.tsv",
  log2_transform = FALSE,
  output_long = TRUE
)

head(result)
}
}
\seealso{
\code{\link[=compute_ibaq]{compute_ibaq()}} for full parameter documentation.
}
