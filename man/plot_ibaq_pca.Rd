% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_ibaq_pca.R
\name{plot_ibaq_pca}
\alias{plot_ibaq_pca}
\title{PCA plot of iBAQ values with KNN-based Louvain clustering}
\usage{
plot_ibaq_pca(
  ibaq,
  samples = NULL,
  log2_values = TRUE,
  k = 5,
  components = c(1, 2),
  point_size = 3,
  label_samples = TRUE,
  scale = TRUE,
  color_palette = NULL
)
}
\arguments{
\item{ibaq}{A \code{data.frame} or \code{tibble} returned by \code{\link[=compute_ibaq]{compute_ibaq()}}, in
either wide or long format.}

\item{samples}{\code{character} vector or \code{NULL}. Samples to include. When
\code{NULL} (default), all samples are used.}

\item{log2_values}{\code{logical(1)}. Are values already log2-transformed?
Affects only the subtitle annotation. Default: \code{TRUE}.}

\item{k}{\code{integer(1)}. Number of nearest neighbours for the KNN graph.
Default: \code{5}. If \code{k} is greater than or equal to the number of samples,
it is automatically reduced to \code{n_samples - 1}.}

\item{components}{\code{integer(2)}. Which principal components to plot.
Default: \code{c(1, 2)} (PC1 vs PC2).}

\item{point_size}{\code{numeric(1)}. Size of the scatter points.
Default: \code{3}.}

\item{label_samples}{\code{logical(1)}. If \code{TRUE}, annotate each point with
the sample name using \code{\link[ggrepel:geom_text_repel]{ggrepel::geom_text_repel()}}. Default: \code{TRUE}.}

\item{scale}{\code{logical(1)}. Whether to scale variables to unit variance
before PCA. Default: \code{TRUE}.}

\item{color_palette}{\code{character} or \code{NULL}. A character vector of colours
for cluster assignments, or \code{NULL} to use a default palette.
Default: \code{NULL}.}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2}} object.
}
\description{
Performs \strong{principal component analysis (PCA)} on the iBAQ protein
abundance matrix (samples as observations, proteins as features) and
displays the results as a 2D scatter plot. Samples are coloured by
cluster assignments derived from \strong{K-nearest-neighbour (KNN) graph}
construction followed by \strong{Louvain community detection}.

This visualisation is useful for assessing sample-level similarity,
detecting batch effects, and identifying outlier runs.
}
\examples{
\dontrun{
ibaq <- compute_ibaq("report.parquet", "proteome.fasta")

# Default PCA with KNN clustering
plot_ibaq_pca(ibaq)

# Custom k and no sample labels
plot_ibaq_pca(ibaq, k = 3, label_samples = FALSE)

# Plot PC2 vs PC3
plot_ibaq_pca(ibaq, components = c(2, 3))
}
}
