% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_theoretical_peptide_counts.R
\name{build_theoretical_peptide_counts}
\alias{build_theoretical_peptide_counts}
\title{Build a table of theoretical tryptic peptide counts for a proteome}
\usage{
build_theoretical_peptide_counts(
  sequences,
  protease_regex = "(?<=[KR])(?!P)",
  min_len = 6,
  max_len = 30,
  max_missed = 0
)
}
\arguments{
\item{sequences}{Named \code{character} vector. Names are protein accessions,
values are amino-acid sequences. Typically the output of \code{\link[=parse_fasta]{parse_fasta()}}.}

\item{protease_regex}{\code{character(1)}. Perl-compatible regex defining the
cleavage rule. Default: \code{"(?<=[KR])(?!P)"} (trypsin, no cut before Pro).}

\item{min_len}{\code{integer(1)}. Minimum peptide length to count. Default: \code{6}.}

\item{max_len}{\code{integer(1)}. Maximum peptide length to count. Default: \code{30}.}

\item{max_missed}{\code{integer(1)}. Maximum missed cleavages. Default: \code{0}.}
}
\value{
A \link[tibble:tibble]{tibble} with two columns:
\describe{
\item{\code{protein_id}}{\code{character} — protein accession.}
\item{\code{n_theoretical_peptides}}{\code{integer} — number of theoretically
observable tryptic peptides within the specified length window.}
}
}
\description{
Applies \code{\link[=count_theoretical_peptides]{count_theoretical_peptides()}} to every sequence in a named
character vector (as returned by \code{\link[=parse_fasta]{parse_fasta()}}) and returns a tidy tibble
with one row per protein. This table provides the denominators for the iBAQ
calculation.
}
\seealso{
\code{\link[=parse_fasta]{parse_fasta()}}, \code{\link[=compute_ibaq]{compute_ibaq()}}
}
\keyword{internal}
