% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_ibaq_rank.R
\name{plot_ibaq_rank}
\alias{plot_ibaq_rank}
\title{Plot ranked protein abundances per sample}
\usage{
plot_ibaq_rank(
  ibaq,
  samples = NULL,
  log2_values = TRUE,
  top_n_labels = 10,
  label_col = "Protein.Names",
  point_size = 0.8,
  point_alpha = 0.6,
  color_palette = NULL
)
}
\arguments{
\item{ibaq}{A \code{data.frame} or \code{tibble} returned by \code{\link[=compute_ibaq]{compute_ibaq()}}.
Accepted in both \strong{wide} format (default output, one column per sample)
and \strong{long} format (\code{output_long = TRUE}, columns \code{sample} and \code{iBAQ}).}

\item{samples}{\code{character} vector or \code{NULL}. Names of samples (columns in
wide format, or values in the \code{sample} column of long format) to include.
When \code{NULL} (default), all samples are plotted.}

\item{log2_values}{\code{logical(1)}. Are the iBAQ values already
log2-transformed (i.e., \code{log2_transform = TRUE} was used in
\code{\link[=compute_ibaq]{compute_ibaq()}})? This only affects the y-axis label. Default: \code{TRUE}.}

\item{top_n_labels}{\code{integer(1)}. Number of top-ranked proteins (by iBAQ
value, per sample) to annotate with text labels. Set to \code{0} to suppress
all labels. Default: \code{10}.}

\item{label_col}{\code{character(1)}. Name of the column in \code{ibaq} to use as
the protein label text. Falls back to \code{"protein_id"} if the specified
column is absent. Default: \code{"Protein.Names"}.}

\item{point_size}{\code{numeric(1)}. Size of the scatter points. Default: \code{0.8}.}

\item{point_alpha}{\code{numeric(1)}. Opacity (0–1) of the scatter points.
Default: \code{0.6}.}

\item{color_palette}{\code{character} or \code{NULL}. A named character vector mapping
sample names to colours, or \code{NULL} to use the default ggplot2 colour
scale. Default: \code{NULL}.}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2}} object. Use \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} to
save to disk, or display it directly in an interactive R session.
}
\description{
Visualises protein abundances as a \strong{rank-abundance (Whittaker) plot}:
proteins are ranked from most to least abundant along the x-axis, and their
iBAQ values are shown on the y-axis. One panel is drawn per sample when
multiple samples are selected (\code{facet_wrap}).

This plot is useful for assessing the dynamic range of quantification and
for identifying the most abundant proteins in each sample.
}
\examples{
\dontrun{
ibaq <- compute_ibaq("report.parquet", "proteome.fasta")

# ── All samples, default labels ─────────────────────────────────────────────
p <- plot_ibaq_rank(ibaq)
print(p)

# ── Subset to two samples, label top 5 proteins ────────────────────────────
p2 <- plot_ibaq_rank(
  ibaq,
  samples = c("sample_A", "sample_B"),
  top_n_labels = 5,
  label_col = "Genes"
)
ggplot2::ggsave("rank_abundance.pdf", p2, width = 10, height = 5)
}
}
