% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_diann_report.R
\name{filter_diann_report}
\alias{filter_diann_report}
\title{Apply quality-control filters to a DIA-NN report}
\usage{
filter_diann_report(
  report,
  q_value_cutoff = 0.01,
  pg_q_value_cutoff = 0.01,
  lib_pg_q_value_cutoff = 0.01,
  proteotypic_only = TRUE,
  lfq_quality_cutoff = 0.5
)
}
\arguments{
\item{report}{A \code{data.frame} or \code{tibble} produced by
\code{\link[arrow:read_parquet]{arrow::read_parquet()}} on a DIA-NN \code{report.parquet} file.}

\item{q_value_cutoff}{\code{numeric(1)} or \code{NULL}. Maximum allowed precursor-level
false discovery rate (\code{Q.Value} column). Set to \code{NULL} to skip this filter.
Default: \code{0.01} (1 \% FDR).}

\item{pg_q_value_cutoff}{\code{numeric(1)} or \code{NULL}. Maximum allowed
protein-group-level FDR (\code{PG.Q.Value} column). Set to \code{NULL} to skip.
Default: \code{0.01}.}

\item{lib_pg_q_value_cutoff}{\code{numeric(1)} or \code{NULL}. Maximum allowed
library protein-group-level FDR (\code{Lib.PG.Q.Value} column). Set to \code{NULL}
to skip. Default: \code{0.01}.}

\item{proteotypic_only}{\code{logical(1)}. If \code{TRUE} (default), retains only rows
where \code{Proteotypic == 1}, i.e. unique peptides unambiguously mapped to a
single protein. Recommended for accurate iBAQ quantification.}

\item{lfq_quality_cutoff}{\code{numeric(1)} or \code{NULL}. Minimum LFQ quality score
(\code{PG.MaxLFQ.Quality} column). Set to \code{NULL} or \code{0} to skip. Default: \code{0.5}.}
}
\value{
A filtered \code{tibble} with the same columns as \code{report} but fewer rows.
A summary of rows removed is printed to the console via \code{\link[=message]{message()}}.
}
\description{
Filters a DIA-NN report data frame (loaded from \code{report.parquet}) according
to standard quality thresholds used in proteomics data analysis. The default
thresholds follow DIA-NN best-practice recommendations and the
iBAQ-for-DIANN reference implementation.

Filters are applied only when the corresponding column exists in \code{report},
so the function is robust to different DIA-NN output versions.
}
\examples{
\dontrun{
report <- arrow::read_parquet("report.parquet")

# Default filters (1 \% FDR, proteotypic, LFQ quality >= 0.5)
filtered <- filter_diann_report(report)

# Relaxed: no LFQ quality filter, allow non-proteotypic peptides
filtered_relaxed <- filter_diann_report(
  report,
  lfq_quality_cutoff = NULL,
  proteotypic_only = FALSE
)
}
}
