% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_ibaq_pie.R
\name{plot_ibaq_pie}
\alias{plot_ibaq_pie}
\title{Plot proportional iBAQ abundance as a pie chart}
\usage{
plot_ibaq_pie(
  ibaq,
  samples = NULL,
  log2_values = TRUE,
  top_n = 10,
  label_col = "Protein.Names",
  show_labels = TRUE,
  show_names = TRUE,
  color_palette = NULL
)
}
\arguments{
\item{ibaq}{A \code{data.frame} or \code{tibble} returned by \code{\link[=compute_ibaq]{compute_ibaq()}}, in
either wide or long format.}

\item{samples}{\code{character} vector or \code{NULL}. Samples to include. When
\code{NULL} (default), all samples are plotted.}

\item{log2_values}{\code{logical(1)}. Are the iBAQ values already
log2-transformed (i.e., \code{log2_transform = TRUE} was used in
\code{\link[=compute_ibaq]{compute_ibaq()}})? When \code{TRUE} (default), the function back-transforms
via \code{2^x - 1} before computing proportions.}

\item{top_n}{\code{integer(1)}. Number of top proteins (by proportional
abundance) to display as individual slices. All remaining proteins are
collapsed into an "Other" slice. Default: \code{10}.}

\item{label_col}{\code{character(1)}. Column in \code{ibaq} to use for protein
labels. Falls back to \code{"protein_id"} if the column is absent.
Default: \code{"Protein.Names"}.}

\item{show_labels}{\code{logical(1)}. Show percentage labels on slices?
Default: \code{TRUE}.}

\item{show_names}{\code{logical(1)}. Show protein name labels for the top-N
slices using \code{\link[ggrepel:geom_text_repel]{ggrepel::geom_text_repel()}}? Default: \code{TRUE}.}

\item{color_palette}{\code{character} or \code{NULL}. A character vector of colours
(length >= \code{top_n + 1}), or \code{NULL} to use a default pastel palette.
Default: \code{NULL}.}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2}} object.
}
\description{
Draws a \strong{pie chart} of proportional iBAQ (riBAQ) values per sample.
Each protein's contribution is expressed as a percentage of the total
sample abundance:

\deqn{\text{riBAQ}_i = \frac{\text{iBAQ}_i}{\sum_j \text{iBAQ}_j} \times 100}

Only the \code{top_n} most-abundant proteins are shown as individual slices;
the remaining proteins are grouped into an "Other" category. Optionally,
protein names (or gene symbols) can be displayed on the slices using
\code{\link[ggrepel:geom_text_repel]{ggrepel::geom_text_repel()}}.
}
\examples{
\dontrun{
ibaq <- compute_ibaq("report.parquet", "proteome.fasta")

# Default pie chart for all samples
plot_ibaq_pie(ibaq)

# Show top 5 proteins, label by gene name
plot_ibaq_pie(
  ibaq,
  top_n = 5,
  label_col = "Genes",
  samples = c("sample_A", "sample_B")
)
}
}
