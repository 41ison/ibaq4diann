% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_ibaq_dynamic_range.R
\name{plot_ibaq_dynamic_range}
\alias{plot_ibaq_dynamic_range}
\title{Plot the dynamic range and protein detection coverage across samples}
\usage{
plot_ibaq_dynamic_range(
  ibaq,
  samples = NULL,
  log2_values = TRUE,
  show_n_proteins = TRUE,
  point_size = 3,
  segment_linewidth = 1.2,
  color_palette = NULL
)
}
\arguments{
\item{ibaq}{A \code{data.frame} or \code{tibble} returned by \code{\link[=compute_ibaq]{compute_ibaq()}}, in
either wide or long format.}

\item{samples}{\code{character} vector or \code{NULL}. Samples to include. When
\code{NULL} (default), all samples are shown.}

\item{log2_values}{\code{logical(1)}. Are values already log2-transformed?
Affects only the y-axis label. Default: \code{TRUE}.}

\item{show_n_proteins}{\code{logical(1)}. Annotate each sample bar with the
number of quantified proteins. Default: \code{TRUE}.}

\item{point_size}{\code{numeric(1)}. Size of the median point. Default: \code{3}.}

\item{segment_linewidth}{\code{numeric(1)}. Width of the range segment line.
Default: \code{1.2}.}

\item{color_palette}{\code{character} or \code{NULL}. Named vector mapping sample
names to colours, or \code{NULL} to use the default ggplot2 palette.
Default: \code{NULL}.}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2}} object.
}
\description{
Displays the \strong{dynamic range} of iBAQ quantification per sample: the
minimum, median, and maximum iBAQ value are annotated, and the fraction of
quantified proteins is indicated. This is a useful QC visualisation for
assessing proteome coverage and signal dynamic range across runs.

Each sample is represented as a vertical segment spanning its iBAQ value
range, with a point at the median. A rug on the y-axis shows the full
distribution.
}
\examples{
\dontrun{
ibaq <- compute_ibaq("report.parquet", "proteome.fasta")

# Default dynamic range plot
plot_ibaq_dynamic_range(ibaq)

# Subset to specific samples, hide protein count labels
p <- plot_ibaq_dynamic_range(
  ibaq,
  samples          = c("sample_A", "sample_B"),
  show_n_proteins  = FALSE
)
ggplot2::ggsave("dynamic_range.pdf", p, width = 8, height = 6)
}
}
