% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_ibaq_correlation.R
\name{plot_ibaq_correlation}
\alias{plot_ibaq_correlation}
\title{Plot a sample-to-sample correlation heatmap of iBAQ values}
\usage{
plot_ibaq_correlation(
  ibaq,
  samples = NULL,
  method = c("pearson", "spearman"),
  log2_values = TRUE,
  low_color = "#f7fbff",
  high_color = "#08306b",
  show_values = TRUE,
  digits = 2
)
}
\arguments{
\item{ibaq}{A \code{data.frame} or \code{tibble} returned by \code{\link[=compute_ibaq]{compute_ibaq()}}, in
either wide or long format.}

\item{samples}{\code{character} vector or \code{NULL}. Samples to include. When \code{NULL}
(default), all samples are used.}

\item{method}{\code{character(1)}. Correlation method: \code{"pearson"} (default) or
\code{"spearman"}. Passed to \code{\link[stats:cor]{stats::cor()}}.}

\item{log2_values}{\code{logical(1)}. Are values already log2-transformed?
When \code{FALSE}, a \code{log2(x + 1)} transformation is applied before computing
correlations. Default: \code{TRUE}.}

\item{low_color}{\code{character(1)}. Colour for low correlation values.
Default: \code{"#f7fbff"} (near-white blue).}

\item{high_color}{\code{character(1)}. Colour for high correlation values.
Default: \code{"#08306b"} (dark blue).}

\item{show_values}{\code{logical(1)}. If \code{TRUE}, print the correlation
coefficient inside each tile. Default: \code{TRUE}.}

\item{digits}{\code{integer(1)}. Number of decimal places for displayed
correlation values. Default: \code{2}.}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2}} object.
}
\description{
Computes pairwise Pearson (or Spearman) correlations of log-transformed iBAQ
values across all selected samples and displays the result as a colour-coded
heatmap. This is a standard quality-control visualisation for assessing
reproducibility between biological or technical replicates.
}
\examples{
\dontrun{
ibaq <- compute_ibaq("report.parquet", "proteome.fasta")

# Default Pearson correlation heatmap
plot_ibaq_correlation(ibaq)

# Spearman correlation, no value labels
p <- plot_ibaq_correlation(
  ibaq,
  method      = "spearman",
  show_values = FALSE
)
ggplot2::ggsave("correlation_heatmap.pdf", p, width = 8, height = 7)
}
}
